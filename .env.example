# Only needed for Groq (free cloud provider).
# Get a free key at https://console.groq.com — no credit card required.
#
# If you're using Ollama (local), no .env file is needed at all.

GROQ_API_KEY=your_groq_api_key_here
